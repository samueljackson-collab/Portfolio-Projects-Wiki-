# Wiki Generator Dependencies
# ============================

# YAML config parsing
PyYAML>=6.0

# No other external dependencies required!
# The pipeline uses Python stdlib for HTTP requests (urllib)
# to avoid heavy dependencies and keep the footprint minimal.
#
# Optional (uncomment if preferred over stdlib urllib):
# requests>=2.31
# httpx>=0.27
